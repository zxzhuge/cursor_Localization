# Cursor 完整汉化工具分享：一键把界面变成中文

> **💡操作提示：**本工具面向日常使用 Cursor 的用户，在不修改 Cursor 核心程序的前提下，将界面中的英文菜单、按钮、提示和说明替换为中文，降低阅读和操作成本。已实现 编辑器模式（Editor）和 智能体模式（Agent）两种界面的全面汉化。
---

## 工具介绍

Cursor 官方目前尚未提供中文界面或汉化包，常见做法是安装VSCode汉化插件实现部分汉化，菜单、通用设置等会变成中文，但智能体侧栏、Cursor 设置、插件市场等 **Cursor 专有界面** 往往仍是英文，给习惯使用中文界面的用户增加了学习和使用成本。

**Cursor 完整汉化工具** 通过向工作台注入带词典的运行时脚本，把**Cursor专有界面**替换成中文，让日常使用的界面尽量一眼能读懂。**不要求**安装 Chinese (Simplified) Language Pack。工具已实现编辑器模式（Editor）和智能体模式（Agent）两种界面的全面汉化。

### 发布地址：

[https://www.vibepm.net/cursor/](https://www.vibepm.net/cursor/)

### 覆盖区域：

- **设置页**：账户、模型、规则、工具与 MCP、隐私等选项
- **智能体窗口**：对话、命令面板、模式切换（计划 / 询问 / 调试 / 多任务）等
- **插件市场**：插件名称、简介、获取按钮及市场相关文案
- **其它界面**：欢迎页、Git 变更面板、部分通知与推广弹窗等

### 主要特点：

- **无需官方语言包**：默认不安装、不依赖 Chinese (Simplified) Language Pack；仅通过注入脚本完成汉化。
- **注入汉化，覆盖 Cursor 专有界面**：通过注入脚本 + 本地词典，翻译智能体侧栏、Cursor 设置（账户 / 模型 / 规则 / MCP）、插件市场、欢迎页、Git 面板及部分通知弹窗等。
- **不改动核心程序，可一键还原**：只修改工作台相关文件（如 `workbench.html`），需要英文时运行「取消汉化」脚本即可恢复。
- **维护成本低**：Cursor 小版本更新后若有个别位置变回英文，再运行一次启动脚本即可，一般无需重装软件。
- **其它说明**：日常点得到的界面大多已是中文；个别菜单、动态文案或新版本新增词条可能仍为英文，可在词典中补充后重新注入。Agent 对话与模型生成内容不属于界面汉化范围。Markdown 编辑器标签旁的 **Preview** 预览按钮保持英文，不翻译。

---

## 如何使用

### 环境要求

汉化工具依赖 **Python 3.8+**（Windows 通过 `启动汉化_Win.bat` 调用，Mac/Linux 通过 shell 脚本调用）。

1. 下载并解压汉化工具（内含 `启动汉化_Win.bat` / `启动汉化_Mac.sh` 等）。
2. **Windows**：双击 `启动汉化_Win.bat`。**Mac / Linux**：在终端执行 `启动汉化_Mac.sh`。
3. 脚本会注入汉化（**不要求**安装官方简体中文语言包），按提示可选打开 Cursor。
4. **完全退出并重启** Cursor（不要只重载窗口）。

**Mac在终端中运行汉化脚本：**

1. 打开**终端**：按 `Cmd + 空格`，输入「终端」后回车。
2. 在终端输入 `bash`加空格，再把 `启动汉化_Mac.sh` 拖入窗口补全路径后回车，输入`Y`进行汉化。

本工具**不要求**安装 **Chinese (Simplified) Language Pack**。

## 恢复英文

Windows 双击 `取消汉化_Win.bat`； Mac / Linux 运行 `取消汉化_Mac.sh`；

同样要完全退出并重启。取消汉化只撤销注入并恢复备份文件。

---

## 常见问题

- **更新 Cursor 后汉化失效了**：重新再运行一次汉化脚本即可。
- **已经执行汉化但是没有生效**：确认已完全重启了 Cursor。
- **运行脚本报错 / 弹出 Microsoft Store / 汉化无输出**：通常是未安装真正的 Python。运行 `where python`，若路径含 `Microsoft\WindowsApps`，请从 [python.org](https://www.python.org/downloads/) 安装 Python 3 并勾选 **Add python.exe to PATH**，或执行 `winget install Python.Python.3.12`；安装后重开终端，用 `python --version` 确认。
- **提示「安装已损坏 / installation appears to be corrupt」**：汉化会修改 `workbench.html`，Cursor 还会校验 `product.json` 中的**全部** checksum（约 6 个核心文件）。请运行 `修复校验_Win.bat`（Mac/Linux 用 `修复校验_Mac.sh`），或执行 `python Cursor_Localization_Tool.py --fix-checksum`。若提示权限不足，Windows 请右键**以管理员身份运行**；完成后完全退出并重启 Cursor。
- **标准菜单仍是英文**：本工具默认不安装官方语言包；若需要 VS Code 标准菜单也中文化，可自行安装 Language Pack，或运行 `python Cursor_Localization_Tool.py --install-langpack`（可选，非必需）。
- 用记事本打开目录中的 `Cursor_Localization_Tool.py`，在文件开头 `cd /d "%~dp0"`后面增加一行，指定 Cursor 安装目录，保存后重新运行启动脚本。

```bat
set CURSOR_INSTALL_DIR=D:\Program Files\cursor   # 含 Cursor.exe 的安装文件夹
```

- **执行脚本时仍然出错**：直接将汉化脚本拖入到cursor的对话框中，发送：我需要安装这个汉化工具，汉化时出现了报错，帮我检查并重新执行脚本，我安装目录是：XXX

---